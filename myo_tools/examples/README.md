# myo_tools/examples
A set of examples demonstrating `myo_tools` features.

You can call script with `--help` to get a detailed descriptions for each example.
```python
python myo_tools/examples/examine_marker.py --help
```
